{
  "scripts": {
    "dev": "npm --prefix \"RFID Employee Monitor\" run dev -- --host 0.0.0.0 --port 5173",
    "build": "npm --prefix \"RFID Employee Monitor\" run build",
    "preview": "npm --prefix \"RFID Employee Monitor\" run build && npm --prefix \"RFID Employee Monitor\" run dev -- --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@vitejs/plugin-react": "^6.0.1",
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.2"
  }
}